{
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.6",
    "esbuild": "0.20.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "test": "jest",
    "build": "esbuild src/chip8vm/vm.ts --sourcemap --bundle --minify --format=esm --target=chrome100,firefox100 --bundle --outdir=./dist"
  }
}
